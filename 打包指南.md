# 小鸡舰队出击辅助工具 - 打包指南

## 环境要求

- Node.js 16.x 或更高版本
- npm 或 yarn 包管理器
- Windows 操作系统

## 打包步骤

### 步骤1：安装依赖

在命令行中执行以下命令：

```bash
# 进入项目目录
cd f:\code\xjjdcj

# 安装项目依赖
npm install

# 或者使用 yarn
yarn install
```

如果遇到网络问题，可以使用淘宝镜像：

```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 设置Electron镜像
set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

# 重新安装
npm install
```

### 步骤2：验证安装

确保以下依赖已正确安装：

```bash
# 检查Electron
npx electron --version

# 检查electron-builder
npx electron-builder --version
```

### 步骤3：执行打包

```bash
# 执行打包命令
npm run build

# 或者使用 yarn
yarn build
```

### 步骤4：查找打包结果

打包完成后，在 `release` 目录下会生成以下文件：

```
release/
├── 网页嵌入器 Setup 1.0.0.exe    # 安装程序
├── 网页嵌入器 1.0.0.exe          # 便携版（无需安装）
└── win-unpacked/                 # 未打包的文件夹版本
    ├── 网页嵌入器.exe
    ├── resources/
    └── ...
```

## 常见问题

### 1. Electron下载失败

**问题现象**：安装时卡在 `node install.js` 或报错证书错误

**解决方案**：
```bash
# 方法1：使用镜像
set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
npm install

# 方法2：手动下载
1. 访问 https://github.com/electron/electron/releases/tag/v28.0.0
2. 下载 electron-v28.0.0-win32-x64.zip
3. 解压到 node_modules/electron/dist 目录
```

### 2. 打包时提示缺少依赖

**解决方案**：
```bash
# 重新安装所有依赖
rm -rf node_modules
npm install
```

### 3. 打包后的应用无法运行

**检查清单**：
- [ ] 确保所有文件都在 `files` 配置中
- [ ] 检查 `main.js` 路径是否正确
- [ ] 确保 `config.json` 在 `extraFiles` 中

## 测试新版本

打包完成后，请按以下步骤测试周礼包功能：

1. 运行 `release/网页嵌入器 Setup 1.0.0.exe` 安装应用
2. 启动应用，等待游戏加载
3. 登录游戏账号
4. 点击右侧"周礼包"按钮
5. 观察是否：
   - ✅ 打开独立窗口显示礼包页面
   - ✅ 自动点击领取按钮
   - ✅ 领取完成后自动关闭窗口
   - ✅ 返回游戏页面继续游戏

## 分发文件

测试通过后，可以分发以下文件给用户：

### 方式1：安装包（推荐）
```
release/网页嵌入器 Setup 1.0.0.exe
```

### 方式2：便携版
```
release/网页嵌入器 1.0.0.exe
```

### 方式3：文件夹版本
```
release/win-unpacked/  （整个文件夹）
```

## 版本更新说明

### v1.0.0 更新内容

1. **新增周礼包独立窗口功能**
   - 礼包领取在独立窗口中进行
   - 游戏页面始终保持可见
   - 领取完成后自动返回游戏

2. **优化用户体验**
   - 自动领取流程
   - 重试机制（最多3次）
   - 错误处理和日志记录

3. **技术改进**
   - 新增IPC通信接口
   - 窗口管理和状态同步
   - 多种选择器策略

## 联系方式

如有问题请联系开发者
